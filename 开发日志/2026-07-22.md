# 2026-07-22 开发日志

## ✅ 今日完成
- 明确新需求：让别人构建自己的 Agent 时，用 AgentTrace 实现**单次运行透明化**（不知道卡在哪一步 → 一次运行即可定位）。
- 与用户对齐方向：P0=单次运行透明化，优先 LangChain/LangGraph 接入，零改动+装饰器双通道。
- 搭建项目治理脚手架：
  - `docs/`：README 索引 + 01 产品需求 + 02 技术架构 + 03 设计规范 + 04 开发路线图。
  - `开发日志/`：README 说明 + 本日日志。
  - `CLAUDE.md`：标准文件路径指引 + 工作说明。
- 拆分里程碑 M1（单次透明化）为 5 个可验证子步骤（见路线图）。

## 🚧 进行中
- 无（M1.1 已完成，等进入 M1.2）。

## ✅ 今日完成（续 · M1.1）
- 调研录制内核：确认 `TraceCapture.steps` 已录 `id/type/status/error/latency_ms/start_time/end_time` 及各类型输入输出字段 → 无需改内核。
- 定稿 `SingleRunReport` 协议 v1（`docs/02-技术架构.md`），含 type→kind 映射与 status 归一化规则。
- 新增 `agent_obs/single_run.py`：纯逻辑时间线构建器，鸭子类型支持 list/dict/TraceCapture/TraceContext 四种来源。
- 新增 `test_single_run.py`：7 项单测全过（正常/失败/running/耗时/回退/端到端 TraceContext）；现有核心测试无回归。

## ✅ 今日完成（续 · M1.2）
- 新增 `agent_obs/health.py` 健康分析器：失败步骤 / 卡点(running 或 completed=False) / 慢步骤(中位数离群，阈值可配 HealthConfig) + 人话摘要，按需把顶层 status 升级为 failed/stuck。
- 卡点判定落实：running 信号优先，其次调用方告知的异常中断；二者都无则不臆断。
- 新增 `test_health.py`：10 项单测全过（健康/失败/两类卡点/慢步骤/阈值可配/严重度优先级/端到端）。

## ✅ 今日完成（续 · M1.3）
- 探测环境：langchain/langgraph/langchain_openai 均已安装。
- 新增 `examples/single_run_langgraph.py`：真实 StateGraph Agent，DeepSeek 走 OpenAI 兼容接口（key 从环境变量读，不入库）；无 key 自动降级 FakeListChatModel。
- 用真实 DeepSeek 跑通：正常运行→健康报告（4 步/success）；工具抛错→自动定位失败点 weather_api（failed）。mock 路径同样通过。
- 修 Windows 控制台 GBK 崩溃：标记改 ASCII + PYTHONIOENCODING=utf-8。

## ⚠️ 发现的问题（记为 M1.2.1）
- span 每步 `duration_ms`=0：`end_span()` 未重算 latency_ms → 慢步骤检测在真实运行上失效。整段总耗时正常。属录制内核小改，改后需跑 test_v2_compiler / test_explain_engine。

## ✅ 今日完成（续 · M1.2.1）
- 修 `agent_obs/trace_core.py` 的 `end_span()`：用记录时 start_time 重算 end_time/latency_ms。
- 真实 DeepSeek 复验：LLM 步耗时 861/820/694ms（此前恒为 0）。新增计时单测，22 项测试全过（含回归）。
- 记下待观察点：根/父 CHAIN 步含子步耗时，未来慢步骤检测可能需排除容器步骤。

## 📋 待办（下次优先）
- **M1.4** 前端「单次运行」时间线视图：把 SingleRunReport 渲染成时间线，高亮失败/卡点/慢步骤。
- M1.4 前调研：看 agent-trace-ui 现有组件与 traceStore/协议类型，尽量复用；确认单次视图如何与现有 A/B 视图并存。

## 📝 备注 / 决策
- 关键判断：现有产品重心是 A/B 对比（需两次运行），而用户痛点是**单次**定位，这是最大能力缺口，故 P0 优先补齐。
- 待用户确认：M1 子步骤拆分与优先级是否认可，确认后即可开工 M1.1。
