# 2026-07-23 开发日志

## ✅ 今日完成
- 合并 `feat/single-run-transparency` 到 `main`（8 个提交，21 文件 1617 行）。
- **M2.1** 实时回调机制：`TraceContext.on_step_end` + `trace_run.on_step`，每步结束时立即回调；5 项单测全过；真实 DeepSeek 验证 4 步实时捕获（2 LLM + 1 工具 + 1 根链）。
- **M2.2** 前端实时订阅：SSE 推送服务器（`stream_server.py`，端口 8766）+ 零依赖前端进度条（`streaming_demo.html`）+ 端到端演示脚本；4 项单测全过；真实 DeepSeek 验证推送正常（4 步入队）。
- **M2.3** 卡住告警：`_TimeoutWatcher` 后台线程监控超时未触发新步，通过 `push_alert_event` 推送告警；5 项单测全过；真实场景验证 1.3s 超时触发告警。
- **M3.1** 因果链自动集成：修改 `TraceDiffer.__init__` 接受可选的 `context_a/context_b`，在 `diff()` 末尾自动调用 `explain_diff()` 填充 `causal_narrative`/`causal_chain`；更新 9 处调用点；3 项单测全过。
- **M3.2** 错误分类强化：新建 `diagnosis.py` 模块，定义 `DiagnosticFeatures` 数据类、`ErrorClassifier` 类（基于特征分类）、`FixSuggester` 类（模板化建议+自定义规则）；集成到 `trace_diff.py` 的 `_classify_error`/`_suggest_fix`；6 项单测全过。
- **M3.3** 变量分析健壮化：新建 `variable_analysis.py` 模块，定义 `VariableAnalyzer` 类（直接从步骤 `produces` 字段提取变量）、`VariableDiff` 数据类（格式化输出）；集成到 `trace_diff.py` 的 `_extract_root_variable`；8 项单测全过。
- **M3.4** 渲染整合与测试：新建 `diff_renderer.py` 模块，定义 `DiffRenderer` 类整合 `render_diff`/`render_causal_verdict`，支持 4 种格式（executive/engineer/debug/verdict）+ dict 输出；保留 `render_diff`/`render_causal_verdict` 向后兼容；新增 `test_trace_diff.py`（10 项）和 `test_diff_renderer.py`（10 项）单测全过。

## 🚧 进行中
- 无（M3 全部 4 步完成）。

## 📋 待办（下次优先）
- M3 已完成。后续方向可选 **M1.6 融入 Vue DevTools** / **其他 P2 能力**。

## 📝 备注 / 决策
- M3.1 发现 `_extract_causal_chain` 解析 bug：`line.strip().startswith("  =")` 永远 False，修复为检查原始行前导空格。
- M3.2 修复重试模式检测逻辑：要求两个 run 都有多个错误才认为是重试循环。
- M3.3 变量优先级：`selected_tool` > `*_result` > `plan` > `intent` > `input.*` > `other`。
- M3.4 保留旧函数签名向后兼容；修复 `_extract_root_variable_from_narrative` docstring 未闭合导致的 SyntaxError。
